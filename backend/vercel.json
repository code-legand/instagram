{
    "builds": [{
        "src": "backend/wsgi.py",
        "use": "@ardnt/backend-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/wsgi.py"
        }
    ]
}
